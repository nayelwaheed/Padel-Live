const state={screen:"matches",gender:"men",favourites:new Set(JSON.parse(localStorage.getItem("padel-live-favourites")||"[]"))};const $=s=>document.querySelector(s),$$=s=>[...document.querySelectorAll(s)];function allPlayers(){const m=new Map();[...PADEL_DATA.rankings.men,...PADEL_DATA.rankings.women].forEach(p=>m.set(p.name,p));return[...m.values()]}function save(){localStorage.setItem("padel-live-favourites",JSON.stringify([...state.favourites]));renderRankings();renderFollowing()}function toggle(name){state.favourites.has(name)?state.favourites.delete(name):state.favourites.add(name);save()}function renderFeatured(){const m=PADEL_DATA.featuredMatch;$("#featuredMatch").innerHTML=`<div class="match-top"><span class="${m.status==="live"?"status-live":"status-upcoming"}">${m.status==="live"?"LIVE":"NEXT"}</span><span class="round">${m.round}</span></div><div class="score-grid"><div><div class="team">${m.teamA.join(" / ")}</div><div class="team">${m.teamB.join(" / ")}</div></div><div><div class="score-row">${m.scoreA.map((s,i)=>`<span class="score-box ${i===2&&m.status==="live"?"current":""}">${s}</span>`).join("")}</div><div class="score-row" style="margin-top:7px">${m.scoreB.map((s,i)=>`<span class="score-box ${i===2&&m.status==="live"?"current":""}">${s}</span>`).join("")}</div></div></div><div class="match-footer"><span>${m.time}</span><button class="watch-btn" id="featuredAlertBtn">Remind me</button></div>`;$("#featuredAlertBtn").onclick=()=>requestAlerts(true)}function renderToday(){$("#todayMatches").innerHTML=PADEL_DATA.todayMatches.map(m=>`<article class="match-card"><div class="match-card-row"><div class="teams"><div class="sub">${m.division}</div><strong>${m.teamA}</strong><strong>${m.teamB}</strong></div><span class="time">${m.time}</span></div></article>`).join("")}function renderTournaments(){$("#tournamentList").innerHTML=PADEL_DATA.tournaments.map(t=>`<article class="tournament-card"><div><div class="country">${t.country}</div><h3>${t.name}</h3></div><div><div class="dates">${t.dates}</div><div class="meta">${t.status}</div></div></article>`).join("")}function renderRankings(){const players=PADEL_DATA.rankings[state.gender];$("#rankingList").innerHTML=players.map(p=>`<div class="ranking-row"><div class="rank-number">${p.rank}</div><div><div class="player-name">${p.name}</div><div class="player-country">${p.country}</div></div><div class="points">${p.points.toLocaleString()}<br><span>pts</span></div><button class="star-button ${state.favourites.has(p.name)?"following":""}" data-player="${encodeURIComponent(p.name)}">${state.favourites.has(p.name)?"★":"☆"}</button></div>`).join("");$$("#rankingList .star-button").forEach(b=>b.onclick=()=>toggle(decodeURIComponent(b.dataset.player)))}function renderFollowing(){const players=allPlayers().filter(p=>state.favourites.has(p.name));$("#followingCount").textContent=`${players.length} player${players.length===1?"":"s"}`;$("#followingEmpty").style.display=players.length?"none":"block";$("#followingList").style.display=players.length?"flex":"none";$("#followingList").innerHTML=players.map(p=>`<div class="following-row"><div class="rank-number">#${p.rank}</div><div><div class="player-name">${p.name}</div><div class="player-country">${p.country}</div></div><div class="points">${p.points.toLocaleString()} pts</div><button class="star-button following" data-player="${encodeURIComponent(p.name)}">★</button></div>`).join("");$$("#followingList .star-button").forEach(b=>b.onclick=()=>toggle(decodeURIComponent(b.dataset.player)))}function showScreen(name){state.screen=name;$$('.screen').forEach(s=>s.classList.toggle('active',s.dataset.screen===name));$$('.nav-item').forEach(n=>n.classList.toggle('active',n.dataset.target===name));window.scrollTo({top:0,behavior:'instant'})}async function requestAlerts(test=false){if(!('Notification'in window)){alert('Notifications are not available in this browser.');return}try{const p=await Notification.requestPermission();if(p==='granted'){if($("#notificationBtn"))$("#notificationBtn").textContent='Enabled';if(test){const reg=await navigator.serviceWorker.ready;await reg.showNotification('Padel Live',{body:'Match alerts are enabled for this device.',icon:'./icons/icon-192.png',badge:'./icons/icon-192.png'})}}else alert('Notification permission was not granted.')}catch(e){alert('On iPhone, add Padel Live to your Home Screen first, then enable notifications from the installed app.')}}function renderAll(){renderFeatured();renderToday();renderTournaments();renderRankings();renderFollowing()}$$('.nav-item').forEach(b=>b.onclick=()=>showScreen(b.dataset.target));$$('.segment').forEach(b=>b.onclick=()=>{state.gender=b.dataset.gender;$$('.segment').forEach(x=>x.classList.toggle('active',x===b));renderRankings()});$("#browsePlayersBtn").onclick=()=>showScreen('rankings');$("#notificationBtn").onclick=()=>requestAlerts(true);$("#testNotificationBtn").onclick=()=>requestAlerts(true);$("#clearFavouritesBtn").onclick=()=>{if(confirm('Remove all followed players?')){state.favourites.clear();save()}};$("#refreshBtn").onclick=()=>{renderAll();$("#refreshBtn").animate([{transform:'rotate(0deg)'},{transform:'rotate(360deg)'}],{duration:450})};const dialog=$("#installDialog");$("#installHelpBtn").onclick=()=>dialog.showModal();$(".dialog-close").onclick=()=>dialog.close();dialog.onclick=e=>{if(e.target===dialog)dialog.close()};if('serviceWorker'in navigator)window.addEventListener('load',()=>navigator.serviceWorker.register('./sw.js'));renderAll();